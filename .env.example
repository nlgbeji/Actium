# Actium Configuration
# Copy this file to .env and update the values as needed

# =============================================================================
# Optional Configuration (RuntimeConfig)
# =============================================================================

# 最大执行步数 - Agent 在执行任务时的最大步数（默认: 20）
# 仅在未显式指定时生效
ACTIUM_MAX_STEPS=20

# 执行超时时间（秒）- 单次代码执行的超时时间（默认: 300）
# 仅在未显式指定时生效
ACTIUM_TIMEOUT=300

# =============================================================================
# SimpleLLMFunc Configuration
# =============================================================================

# Provider 配置文件路径 - LLM 提供者配置文件路径（默认: provider.json）
# 如果未设置，将使用默认值 "provider.json"
SIMPLELLMFUNC_PROVIDER_CONFIG=provider.json

# =============================================================================
# SimpleLLMFunc Logging Configuration
# =============================================================================

# 日志级别 - 控制台日志级别（默认: WARNING）
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
# 注意：只控制控制台日志的输出，不会影响文件日志的输出
LOG_LEVEL=WARNING

# 日志目录 - 日志文件存储目录（默认: ./logs）
LOG_DIR=./logs

# =============================================================================
# SimpleLLMFunc Langfuse Configuration (Optional)
# =============================================================================

# Langfuse 基础 URL - Langfuse 服务地址（可选）
# 如果未设置，Langfuse 集成将被禁用
LANGFUSE_BASE_URL=http://your.langfuse.url

# Langfuse 公钥 - Langfuse 公钥（可选）
# 如果未设置，Langfuse 集成将被禁用
LANGFUSE_PUBLIC_KEY=your_public_key

# Langfuse 密钥 - Langfuse 密钥（可选）
# 如果未设置，Langfuse 集成将被禁用
LANGFUSE_SECRET_KEY=your_secret_key
